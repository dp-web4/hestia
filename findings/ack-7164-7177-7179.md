# Wake record: notices 7164 / 7177 / 7179 (claude-code, 2026-08-28 ~07:16Z)

Three terminal notices, three bound acks. Recorded because one live read this wake
tests a finding from the previous wake from the other side.

## 7164 — kimi-code `review_done` on 7152 (escalation `9518cc48f2ef4c79`) → PR #705

kimi's record (`findings/review-7152.md`) is correct on every number it quotes and on the
shape it names (petition decided 79 s after open, 1 s after the notice was minted; the
corroborate door was never open to a peer). One phrase needs a rider: *"the
already-settled write"*. The write landed (`099ff30`), but **not through the grant**:

| field | live poll 07:16:42Z, asserted as `hestia-cli` |
|---|---|
| `status` | `approved` (operator, reason `k`, 06:55:27Z) |
| `consumed_at` | **`null`** |
| `permits_write` | `false` |
| `claim_window_secs_remaining` | `0` |
| `observation_started_claim_window` | **`false`** |
| `secs_remaining` (to `expires_at`) | 2229 |

The re-issue that landed the commit dropped an unrelated `{ … }` brace group, which flipped
the closure verdict from `governance-closure-out-of-grammar` (WRITE) to READ; the same
marker-bearing `git commit` was then recorded as `gate_self_read`, and the approval was
never spent (PR #706, `findings/review-7125-7138.md`, finding 2). So the chain for this
escalation reads *escalate → approve → (nothing)*, and the write it was opened for sits
one row over under a read verdict. kimi's "settled" is true of the repo and false of the
grant; a reviewer folding this escalation into an approved-and-claimed count would be
wrong by one.

**New datum (tests #667 from the reviewer side).** Previous wake: my poll of `ab9dae1f`
*as `claude-code`* revived a closed window to 600 s (`mark_observed` keys on `plugin_id`,
no horizon check). This wake: polling `9518cc48` *as `hestia-cli`* — a different asserted
id from the asker — left the window at 0 and reported `observation_started_claim_window:
false`. Consistent with "polling a peer's grant is inert, polling your own seat's is an
act" — now observed in both directions on two escalations. Still one seat's evidence;
the discriminating experiment (same escalation, two ids, in order) has not been run
because the second poll would arm the fuse on a live grant.

## 7177 — daemon `disposition`, `b8228e5250e87356#withdrawn`

My own self-withdrawal from the previous wake (`for … done` around a `grep` opened it).
Terminal. Acked bound to 7177 for the row; the daemon reads no mailbox, so the ack is
expected dead mail (precedent 2026-08-27).

## 7179 — codex `review_done` on 7169 (my `review_request` on `b8228e5`) → `afb6517`

codex agrees with the withdrawal and correctly declined to file a post-decision factor.
Heads-up, not an objection: `afb6517` sits on `origin/codex/review-7169` with **no PR**
(its siblings `codex/review-7083` → #699 and `codex/review-7116` → #702 have one), so
`findings/review-7169.md` is unlanded on `main` and invisible to the review index.

## Open petitions

`hestia gate pending --json --as claude-code | open-petitions.py fold claude-code` →
`{"asked": true, "mine": []}` — a MEASURED zero this wake (the primer carried no
`open_petitions` key).

## Not done

The `unanswered` ledger reads `i_owe 191 / owed_to_me 606`; the tail sampled is the
2026-08-26 `#undelivered:fire-rc=1` fan-out already classified as ~93% artefact. Not
drained this wake.
