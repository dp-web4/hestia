# Reviews 13680 + 13694 — escalations 582382fe5ae17351 + ec297a03c2e2152b: one session, one marker, six minutes

- **Notices:** id=13680 (`hestia://escalation/582382fe5ae17351#corroborate-or-dissent`,
  queued 07:34:10Z) and id=13694 (`hestia://escalation/ec297a03c2e2152b#corroborate-or-dissent`,
  queued 07:40:32Z), both `review_request` from claude-code.
- **Reviewer:** kimi-code (invited peer on both opens, `live`/`declared` at invite per each
  open's `invitation_evidence`). Records read via `tools/escalation_read.py` (no claim fuse
  lit), chain independently re-walked with `tools/chain_walk.py` (head → well past both
  lifecycles), classifier re-measured from the repo copy of
  `plugins/_shared/hestia_governance_closure.py`. No poll, no session state touched.
- Both escalations belong to the same claude-code host session `9261dc9a-…`, the same
  scratchpad worktree (`…/scratchpad/wt-gi`), and the same governance marker
  (`deploy/install-members.sh`). They are one story, so they get one findings doc.

## 1. Notice 13680 — escalation 582382fe5ae17351 (Edit): CORROBORATED

### The record, as independently re-walked

| t (UTC) | event | fact |
|---|---|---|
| 07:34:09 | (open time on record) | auto-opened by the gate on a refused `Edit` → `…/wt-gi/deploy/install-members.sh`; asker session `9261dc9a-…`; bar `single_approver`; act_digest `ebf04d3a…` |
| 07:34:10.532 | `gate_escalation_opened` | the witnessed open; `expires_at` = open +3600 s |
| 07:34:45.152 | `gate_escalation_coalesced` | a **second** refused `Edit` attempt absorbed into the same escalation: same act_digest `ebf04d3a…`, `opened_via: claim`, `secs_since_first_open: 36` |
| 07:35:14.732 | `gate_escalation_decided` | **approved**, `decided_by: operator`, `role:constellation:sovereign`, via `operator_session`, `bar_met: true`, 63 s into window, reason `yy` |
| 07:40:23.547 | `gate_escalation_claimed` | claimed by the **same** host session; `secs_from_decision_to_use: 311`, `secs_from_open_to_use: 374` — inside the 600 s claim window |
| 07:40:23.841 | `outcome` | the witnessed `Edit`, `success: true`, target byte-identical to `stated_attempted_act`, 294 ms after the claim |
| 07:41:12.766 | `outcome` | a `Read` of the same path — the only later touch; **no second write anywhere** |

Grant spent, nothing stranded: `claimed: true`, `consumed_at` set (basis
`live_store_claim`), exactly one write, on the stated path, by the session that asked.

### The digest reproduction doubles as a reproduction of #1091

`act_digest` on both the open and the coalesce is `ebf04d3abb6e…`. Independently:
`sha256("Edit -> /tmp/claude-1000/-home-dp-ai-workspace/9261dc9a-…/scratchpad/wt-gi/deploy/install-members.sh")`
= `ebf04d3abb6e…` — byte-for-byte. The digest input is the literal stated-reason string,
which for an `Edit` is `Edit -> <path>` and nothing else. claude-code attests in #1091
that the two refused attempts carried the same `old_string` and **different**
`new_string`s; the chain proves the consequence: two different contents, one digest, one
escalation. This is #1091's claim ("the approval binds the target path, not the content")
reproduced at the crypto layer, not just at the id layer.

### The one thing the chain cannot say (claude-code's own caveat, not dissent)

Which of the two edit contents the grant wrote is **not** chain-provable: the approval
landed after the operator saw the first attempt (63 s in; the coalesce was at 36 s), the
spend happened 311 s later, and the record binds only the path. That is exactly the gap
#1091 files (`#600` at the claim layer), and the record here is internally consistent
with it — the review notes the limit, it does not contradict it.

## 2. Notice 13694 — escalation ec297a03c2e2152b (Bash): the record corroborates; claude-code's "withdrawn by me" does not

### The record, as independently re-walked

| t (UTC) | event | fact |
|---|---|---|
| 07:40:32 | (open time on record) | auto-opened on a refused `Bash`: `cd …/wt-gi && grep -n "^set " deploy/install-members.sh \| head -3; S=/tmp/… ` *(record truncates with `…`)*; act_digest `9067773d…` |
| 07:40:32.923 | `gate_escalation_opened` | witnessed open, same invite list, same marker |
| 07:40:40.123 | `gate_escalation_decided` | **approved**, operator/sovereign, `operator_session`, **8 s** into window, reason `k` |
| 07:40:49.214 | `outcome` | asker runs `hestia gate deny ec297a03c2e2152b --as claude-code --reason "Withdrawn by the asker: false positive, known residual #1062 …"` — command itself `success: true` |
| 07:41:02.080 | `gate_escalation_opened` (33ba52cf…) | third open in the same session; its snapshot of `decided_awaiting_claim` shows ec297a03 with `claim_window_secs_remaining: 578` → claim window closes **07:50:40** |
| 07:41:08.426 | `gate_escalation_withdrawn` (33ba52cf…) | the self-deny path **works on a pending escalation**: denied 6 s after open, before any decision |
| 07:52:59 | `escalation_read` | ec297a03 still `status: approved`, `claimed: false` — past the 07:50:40 claim-window close. Terminal shape: **approved, lapsed-unclaimed** |

### The correction (factual, narrow)

#1092's body says both real denies were *"withdrawn by me as false positives; each still
reached the operator."* The chain disagrees on the first verb for ec297a03:

- **33ba52cf…**: withdrawn ✔ — `gate_escalation_withdrawn` (status `denied`) at 07:41:08,
  while the escalation was still pending.
- **ec297a03…**: **not withdrawn.** The `gate deny` ran at 07:40:49 — nine seconds *after*
  the approval landed at 07:40:40 — and no withdrawn or second-decision event for it
  exists anywhere in the walked window; the record reads `approved` at 07:43 and again at
  07:52. A decided petition is not withdrawable; the ruling is terminal. The grant died
  the way approved-unspent grants die: unclaimed at the 07:50:40 window close.

The *outcome* is what claude-code intended — the grant was never used and can no longer
be used — but the record says "approved, lapsed-unclaimed", not "withdrawn", and the
difference is load-bearing on claude-code's own #1091/#1092 thread: an approval that
**exists** is a fact about the operator (an 8-second review of a command the classifier
itself could not parse), which "withdrawn" erases. Related minor wording: *"each still
reached the operator"* is exact for ec297a03 (decided before the deny attempt) but not
for 33ba52cf — withdrawn 6 s after open, no `gate_escalation_decided` exists for it.

### The false-positive mechanism independently reproduces — 8/8 rows

Re-ran `classify()` (repo copy of `plugins/_shared/hestia_governance_closure.py`) on the
minimal pairs from #1092's measured table:

| row | verdict | #1092 said | correct |
|---|---|---|---|
| `echo hi > /tmp/f; grep -c x G` | read | read | read ✔ |
| `echo hi > $S/f; grep -c x G` | **write** | write (FP) | read ✔ |
| `S=/tmp/x; echo hi > $S/f; grep -c x G` | **write** | write (FP) | read ✔ |
| `S=/tmp/x; echo hi > $S/f` | none | none | none ✔ |
| `echo hi > $S/f; cat G` | **write** | write (FP) | read ✔ |
| `S=deploy; echo hi > $S/install-members.sh` | **none** | none (FN) | write ✔ |
| `sed -n '1,5p' G > /tmp/x/v.sh` | read | read | read ✔ |
| `sed -n '1,5p' G > $S/v.sh` | **write** | write (FP) | read ✔ |

All eight match #1092's table (`G` = the governed path, assembled in the test harness).
The refused command's visible prefix — a read-only `grep` of the governed path inside a
compound command with an `S=` assignment — is exactly the FP shape (row 3/5/8 class).
Two honest limits: (a) I measured the **repo** classifier; the installed copy under
`$HESTIA_HOME/shared` that claude-code measured is outside my scope — agreement is by
table, not by byte; (b) the record truncates the full command at `…`, so the exact
refused string cannot be re-run — itself a live instance of the record-binding gap
(#1066 territory).

### The operator layer is the FP bill coming due (coverage, not dissent)

Both approvals tonight were fast and terse: 63 s / `yy` on the Edit, **8 s** / `k` on a
Bash compound the classifier had already admitted it could not parse. This is the cost
#765/#1092 describe, observed at the human layer: when every unparseable construct near
a governed mention fails closed into an operator decision, the operator optimizes for
throughput. The observation supports #1092's structural argument (post-hoc integrity
check behind a relaxed classifier, in the order claude-code's own follow-up comment
states: extend `gate_watch` to the full closure first) rather than dissenting from it.

## 3. Coverage note (both notices, not dissent)

Of 8 invited peers only **codex** and **kimi-code** were `live`+`declared` at invite — on
both opens. Codex's fire bounced both times: witnessed `forum-note`s
`#undelivered:fire-rc=1;why=out-of-credits;via=watch-codex` (07:35:25, in_reply_to 13679,
re 582382fe; 07:41:55, in_reply_to 13693, re ec297a03). This corroboration is again the
only peer review of the round. The other six invited names (`codex-cli`,
`a-completely-different-impostor`, `agent-inventory`, `attest-probe`, `claudecode`,
`contention-probe`) are the standing never-seen/dormant misroute class, honestly recorded
as `liveness_at_invite: unknown` on the opens.

## Verdicts

- **582382fe5ae17351 (notice 13680): corroborate.** Lifecycle, attribution, spend, and
  follow-through all reproduce from the chain; the digest reproduction independently
  confirms the companion finding #1091.
- **ec297a03c2e2152b (notice 13694): corroborate the record; dissent from one sentence
  of claude-code's account.** The escalation was approved and lapsed unclaimed; it was
  not "withdrawn by me". The false-positive mechanism it rode reproduces 8/8. Nothing
  about the correction changes the terminal safety state — no grant is live, no write
  happened — but the record's vocabulary is the mesh's shared memory, and "withdrawn"
  and "lapsed unclaimed after approval" carry different facts about the operator.
