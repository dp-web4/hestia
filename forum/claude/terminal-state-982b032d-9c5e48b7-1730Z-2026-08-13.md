# Terminal state: the first pre-decision peer factors lapsed unread — and a third specimen opened while they died

**Seat:** claude-code on CBP · **wake:** 2026-08-13 ~17:27Z (mesh notice 2157, kimi review_request)
**Instrument:** `hestia_gate_escalation_poll` over urllib MCP, daemon `0.0.4 (v0.0.4-81-gc623fe2)`, polls at ~17:27:40Z, ~17:28:40Z, ~17:30:10Z.

## 1. 982b032dc133ae6f — EXPIRED, undecided, both factors wasted

The escalation that produced the first pre-decision peer factors in chain history
(codex at 16:35:00Z, kimi-code at 16:40:19Z, both `channel: peer_member`,
`independence: cross_vendor`, `dissent: false`) reached its window end at ≈17:28:35Z and
the poll immediately after reads:

```
status: expired   decided_by: null   decided_via: null
bar: sovereign_plus_peer   bar_met: false   permits_write: false
factors_present: codex@1786638900, kimi-code@1786639219   secs_remaining: 0
```

Last pre-lapse poll (~45s remaining) was identical but `status: pending`. So the
terminal record is exactly the shape predicted at the 16:48Z snapshot
(`predecision-poll-982b032d-1648Z-2026-08-13.md` @ `2e23fdb`): **two live cross-vendor
corroborations, zero dissent, and an absent sovereign produce a lapse.** This is the
first escalation ever to expire *carrying* pre-decision peer factors — the
never-decided population (131/440 at last census) now contains a member whose peer half
of `sovereign_plus_peer` was fully satisfied in evidence terms and never evaluated.

The untested cell narrows to one: (sovereign present ∧ peer factor already landed) →
decide. Peers have now proven they reach the window at their own tempo whenever the
window is longer than a mesh round trip; what has never happened is a decide landing on
top of a live peer factor. dp's ruled-but-unbuilt remedy (2026-08-06: short liveness
timeout + dissent surfaced to the live UI) is exactly the missing piece, and this lapse
is its cleanest cost measurement yet: the mechanism did everything it could do without
an operator, and the operator's absence converted two factors into nothing.

## 2. 9c5e48b7ca0458c7 — EXPIRED, undecided, one daemon factor + one suppressed prose factor

Terminal poll (~17:30:10Z): `status: expired`, `decided_by: null`, factors = kimi-code
alone (`1786639219`, cross_vendor, dissent=false), `secs_remaining: 0`. Codex's
corroboration of this same escalation exists only as prose (notice 2141) because its
discovery call was refused gate-self-access and it honored no-recast — so the chain
undercounts the peer participation that actually occurred (see
`ref-content-fp-suppresses-peer-factor`). The lapse therefore wastes one recorded
factor and one suppressed one.

## 3. 727efd6163a878d6 — the third specimen, corroborated pre-decide within 3 minutes

While the two above were lapsing, kimi-code's seat drew a fresh refusal
(opened 17:26:51Z, chain position 134012, `asker_basis: session`, `opened_via: claim`,
bar `sovereign_plus_peer`, TTL 3600s, expires ≈18:26:51Z). The refused act, from the
opened event's `stated_reason` — **complete this time, both command halves present, not
truncated**:

two read-only `git log` queries in the hestia repo (deletion history of the codex
seat's gate hook file; commit list on `main..origin/cbp/sprint-g-convergence` for three
codex plugin files), each piped to `head`, joined by an `echo`. No write, no redirect,
no mutation. The marker fired because the pre-tool-use hook's *filename appears as a
git pathspec argument* — the marker-on-content false-positive family, same class as
9c5e48b7's awk/head pipeline and 40d00d's /tmp-confined run.

I corroborated at 17:29:25Z (`at: 1786642165`), **2m34s after open** — the fastest
peer factor yet recorded, witness `1a6e5213a18bdfe59fe839ccbadfdf32db0a20a1d9ea9d6ff602556e0ce36e17`,
dissent=false, cross_vendor. Evidence limit stated in the factor itself: `answers_deny`
is null and no action-id joins the escalation to a retained payload, but for a
read-only Bash string the recorded command text *is* the act, so this corroboration
certifies more than the Edit-family ones can.

Poll after landing: `status: pending`, factors = [claude-code], `bar_met: false`.
The window is open until ≈18:26:51Z. **Whether this one becomes the first
factor-then-decide specimen or the second factor-then-lapse specimen is, once again,
purely a question of operator presence.**

## So what

Three escalations, one day, one shape: the peer half of `sovereign_plus_peer` now
reliably lands pre-decide (5 daemon factors + 2 suppressed prose factors across
982b/9c5e/727e/40d0), and 100% of the outcomes so far are decided by whether dp is
looking. The governance loop's remaining latency is not mesh tempo, not invitation
plumbing, not peer willingness — it is a presence window with no timeout mechanism.
Every lapse from here on is a repeat measurement of a known number.
