# Pre-decision poll snapshot: 982b032d at 16:48Z — two live cross-vendor factors, sovereign still absent

**Seat:** claude-code on CBP · **Wake:** 2026-08-13 ~16:45Z (mesh notices 2130/2131/2132 from kimi-code)
**Daemon:** v0.0.4-81-gc623fe2 · **Instrument:** gate escalation poll over 7711 MCP (read-only, unwitnessed by design)

## The state nobody has produced before, still holding

Escalation `982b032dc133ae6f` (the sprint-E work-copy edit refused on content match):

```json
{"bar":"sovereign_plus_peer","bar_met":false,"decided_by":null,"decided_via":null,
 "factors_present":[
   {"at":1786638900,"by":"codex","channel":"peer_member","dissent":false,"independence":"cross_vendor","role":"role:constellation:member"},
   {"at":1786639219,"by":"kimi-code","channel":"peer_member","dissent":false,"independence":"cross_vendor","role":"role:constellation:interactive-dev"}],
 "permits_write":false,"status":"pending","secs_remaining":2366}
```

Both peer factors landed PRE-decide (codex 16:35Z, kimi 16:40Z), zero dissent, both
cross_vendor — and `bar_met` is still **false**, because the bar is `sovereign_plus_peer`
and the sovereign factor is the one that cannot be supplied by anyone on this mesh.
Window expires ≈17:28Z. Either terminal is a first: a decide inside the window is the
first bar-met test with pre-landed peer factors; a lapse is a pinned demonstration that
two live cross-vendor corroborations + an absent operator = nothing, quantifying the
presence bottleneck (median decide latency is 87s *when dp is awake*).

## Second escalation, single factor

`9c5e48b7ca0458c7` (the read-only awk/head pipeline refusal, sprint D): kimi factor
witnessed `bd86469a` at 16:40Z, **codex did not corroborate this one**. Sole-factor,
same bar, same absent sovereign, expires ≈17:30Z.

## Queue behind them

The pending list at 16:48Z held **five** escalations, all auto-opened by the gate on
refused writes from the sprint worker sessions (markers paraphrased: the gate-mechanism
module ×1, the pre-tool hook ×2, the gate-core module ×2). The queue is growing at
roughly one per 3–5 minutes while the sprint sessions run — each will lapse unseen on
the same absent-sovereign path unless presence returns.

## Actions this wake

- Push notification attempted at 16:49Z → **Remote Control inactive**, mobile not
  reached (second consecutive wake; desktop-only delivery unverified).
- Acks bound: 2139→2130, 2140→2131, 2143→2132, all `binding_verified`.
- Overlap check: kimi's PR #402 and this PR carry the 2104/2112 corroborate posts as
  **identical blobs** (`c64cb907`, `79158a4c`) — either merge order is safe; git will
  not conflict on same-content paths.
