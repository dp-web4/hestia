{
  "_comment": "Hestia governance hooks for the Google Gemini CLI occupant. Gemini reads hooks from the 'hooks' object in ~/.gemini/settings.json (user), <project>/.gemini/settings.json (project), or an extension-shipped hooks/hooks.json (Extensions priority). INSTALL AT USER LEVEL: Project-source hooks are gated behind isTrustedFolder(), so a project-scoped gate does not run in an untrusted folder. Merge this 'hooks' block into settings.json for a manual install, or ship this file inside a Gemini extension. The BeforeTool gate is FAIL-CLOSED (Gemini's engine fails open on unexpected failure, and exit 1 is ALLOW+warning, so the gate carries a top-level deny-on-exception); it covers run_shell_command + file-write/replace + MCP calls + EGRESS (web_fetch/google_web_search). Only LOCAL read-class tools skip the society-safety governor. Commands bake HESTIA_WORKSPACE because the gate default (~/ai-workspace) is wrong on this host; adjust the absolute paths per install.",
  "_paths_note": "READ THIS BEFORE COPYING. The commands below bake /mnt/c paths -- the exact antipattern the README's Hardening section warns about. They are kept because that is this host's real checkout, and a sample with placeholder paths would be WORSE: a spawn error is a fail-open, so a path that does not exist silently DISARMS the gate rather than failing loudly. Every install must repoint these. The recommended shape is ext4: copy or symlink hooks/ to ~/.gemini/hestia-hooks/ and point the commands there, because the only fail-open surface on this engine is a hook timeout, and a 9p cold-load of a /mnt/c script can exceed it. HESTIA_WORKSPACE may stay on /mnt/c -- it is a data root, not a script load path, so it is not on the timeout path.",
  "_hooksConfig_note": "Pin \"hooksConfig\": {\"enabled\": true} in settings.json alongside this block. It defaults true, but \"enabled\": false disarms every hook and hooksConfig.disabled can disable this gate by command string. Operator's prerogative, but it must be visible to anything auditing the member.",
  "hooks": {
    "BeforeTool": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "type": "command",
            "command": "HESTIA_WORKSPACE=/mnt/c/projects/ai-agents python3 /mnt/c/projects/ai-agents/hestia/plugins/gemini/hooks/before_tool.py",
            "timeout": 15000
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          { "type": "command", "command": "/mnt/c/projects/ai-agents/hestia/plugins/gemini/hooks/observe.sh", "timeout": 15000 }
        ]
      }
    ],
    "AfterTool": [
      {
        "matcher": ".*",
        "hooks": [
          { "type": "command", "command": "/mnt/c/projects/ai-agents/hestia/plugins/gemini/hooks/observe.sh", "timeout": 10000 }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          { "type": "command", "command": "HESTIA_WORKSPACE=/mnt/c/projects/ai-agents /mnt/c/projects/ai-agents/hestia/plugins/gemini/hooks/hydrate.sh", "timeout": 20000 }
        ]
      }
    ]
  }
}
